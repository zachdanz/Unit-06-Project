const qwerty = document.getElementById('qwerty');
const phraseDiv = document.getElementById('phrase');
const missed = 0;
const startButton;

addEventListener.startButton('click', (e) => {
    e.target.parentNode.style.display = "none";
});